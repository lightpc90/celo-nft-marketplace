{
  "name": "marketplace",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ lightpc90/marketplace",
    "create-local": "graph create --node http://localhost:8020/ lightpc90/marketplace",
    "remove-local": "graph remove --node http://localhost:8020/ lightpc90/marketplace",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 lightpc90/marketplace",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.45.0",
    "@graphprotocol/graph-ts": "0.29.1"
  },
  "devDependencies": { "matchstick-as": "0.5.0" }
}
